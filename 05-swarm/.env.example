# Copier en .env :  cp .env.example .env   (.env est gitignoré)
#
# Image de l'app web, DÉJÀ buildée et poussée sur un registre (Swarm ne build pas).
# Remplacez par VOTRE compte / registre, puis :  docker build -t "$WEB_IMAGE" . && docker push "$WEB_IMAGE"
WEB_IMAGE=votre-user/stackdemo:1.0
